import React from "react";
import "./BookShelf.css";

export const BookShelf = () => {
    return (
        <div id="BookShelfDiv">
            <p>Add book into BookShelf to see it here.</p>
        </div>
    );
}